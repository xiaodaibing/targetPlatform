---
kind: dependency_management
name: 依赖管理 — 本仓库为纯文档与规范仓库，不含代码依赖声明
category: dependency_management
scope:
    - '**'
---

经全仓检索，该仓库仅包含 `docs/`（需求、设计、ADR、模板等文档）、`.gitignore`、`CLAUDE.md` 与 `LICENSE`，未发现任何语言级别的依赖声明文件（如 `go.mod`、`package.json`、`requirements.txt`、`Cargo.toml`、`pom.xml`、`build.gradle`、`Gemfile`、`pyproject.toml` 等），也未见 `vendor/`、`node_modules/` 或私有注册表配置。因此，**本仓库不涉及第三方库的依赖管理实践**，本类别不适用于此仓库。