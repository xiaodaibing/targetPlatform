本模块为仓库顶层元数据层,不含任何业务代码,由三个文件构成:
- `CLAUDE.md` 作为「项目宪法」被 Claude Code 在每次会话读取,统一约定后端基于 Hulk(`com.transsion.hulk:hulk-parent:4.0.0.RELEASE`)单体 + Eureka/OpenFeign 的包级有界上下文(dataingest/calc/workbench/tracking)与前端 Vben Admin(pnpm monorepo)技术栈,并规定模块间仅经应用层门面接口协作、对外系统经 Feign 调用、共享契约放 common 包的依赖方向。
- `LICENSE`:采用 Apache License 2.0。
- `.gitignore`:屏蔽 IDE/构建产物/日志/图片及 `/com/transsion` 等内部路径,同时排除 `.claude`、`.cursor`、`.copilot` 等 AI 工具目录。
三者共同约束整个仓库的协作方式与提交范围,不引入运行时依赖。