- 后端继承父 BOM `com.transsion.hulk:hulk-parent:4.0.0.RELEASE`,通过 `hulk-framework-starter-*` 按需引入能力,不在业务 pom 中硬写 Spring/Spring Cloud 版本。
- 应用内模块间协作走应用层门面接口(facade),禁止跨模块直连对方表/Mapper;对外部系统才使用 OpenFeign 声明式客户端。
- 共享 DTO/契约集中放在 `common` 包,作为单体内唯一契约来源,不拆 `-api` 模块。
- 前端强制 pnpm(monorepo + turbo),组件/请求/权限一律使用框架二次封装,禁止裸 fetch 或绕过权限指令直接渲染敏感操作。
- 所有外部配置(连接串、密钥、环境)外部化至 Apollo,严禁硬编码进代码,本地临时配置不得提交。